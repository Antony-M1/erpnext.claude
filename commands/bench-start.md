---
name: bench-start
description: Start the Frappe bench development server
---

Start the ERPNext development server:

1. Run `bench start` in the frappe-bench directory
2. Confirm the server is running on http://localhost:8000
3. If $ARGUMENTS includes "watch", also run `bench watch` for live JS/CSS reload
4. Report any startup errors from the log output
